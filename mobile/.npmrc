# WealthMaster mobile — Expo SDK 52 / React 18.3.1 has peer-dep conflicts
# (e.g. @react-navigation v6 vs v7). Force legacy peer resolution so that
# `npm install` AND `npx expo install` always work without extra flags.
legacy-peer-deps=true
